// funciones
// function nombreDeFuncion(args) ---> args es opcional, y en caso de existir
//                                     los args van separados por coma

// declaracion de funcion
function sumar(a, b) {
  return a + b; 
}

// llamada a la funcion
let resultado = sumar(1, 2);

console.log(resultado);

resultado = sumar(76, 100);
console.log(resultado);